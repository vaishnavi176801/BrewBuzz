specVersion: "v1"
serviceId: "brewbuzz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "brewbuzz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
